{
  "name": "contextd-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "turbo": "latest"
  }
}
